<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.bearer.mapper.ArticleMapper">

    <update id="updatePraise">
        update `cul_article`
        set `praise`=`praise` + 1
        where `id` = #{id}
    </update>

    <update id="updateIsExaminedById">
        update `cul_article`
        set `is_examined` = 1
        where `id` = #{id};
    </update>

    <delete id="deleteById">
        delete from `cul_article` AS `art` where `art`.`id` = #{id}
    </delete>

    <select id="selectList" resultType="org.bearer.entity.vo.Article">
        select `art`.`id`, `art`.`title`, `art`.`picture_url`, `user`.`username`
             , `cui`.`category`, `mat`.`material`
        from `cul_article` AS `art`
            INNER JOIN `cul_user` AS `user`
                ON `art`.`author_id` = `user`.`id`
            INNER JOIN `cul_cuisine` AS `cui`
                ON `cui`.`id` = `art`.`cuisine_id`
            INNER JOIN `cul_material` AS `mat`
                ON `mat`.`id` = `art`.`material_id`
        WHERE `art`.`is_examined` = 0
        ORDER BY `art`.`recommend`
                LIMIT #{start}, #{end};
    </select>

    <select id="selectOne" resultType="org.bearer.entity.vo.Article">
        select `art`.`id`, `art`.`title`, `art`.`article`, `art`.`picture_url`, `art`.`recommend`,
               `art`.`praise`, `user`.`username`, `cui`.`category`, `mat`.`material`
        from `cul_article` AS `art`
            INNER JOIN `cul_user` AS `user`
                ON `art`.`author_id` = `user`.`id`
            INNER JOIN `cul_cuisine` AS `cui`
                ON `cui`.`id` = `art`.`cuisine_id`
            INNER JOIN `cul_material` AS `mat`
                ON `mat`.`id` = `art`.`material_id`
        where `art`.`id` = #{id}
            AND `art`.`is_examined` = 0;
    </select>

    <select id="selectArticleSearchByTitle" resultType="org.bearer.entity.vo.Article">
        select `art`.`id`, `art`.`title`, `art`.`picture_url`, `user`.`username`
             , `cui`.`category`, `mat`.`material`
        from `cul_article` AS `art`
                 INNER JOIN `cul_user` AS `user`
                            ON `user`.`id` = `art`.`author_id`
                 INNER JOIN `cul_cuisine` AS `cui`
                            ON `cui`.`id` = `art`.`cuisine_id`
                 INNER JOIN `cul_material` AS `mat`
                            ON `mat`.`id` = `art`.`material_id`
        where `art`.`title` like "%"#{searchContent}"%";
    </select>

    <select id="selectArticleSearchByAuthorId" resultType="org.bearer.entity.vo.Article">
        select `art`.`id`, `art`.`title`, `art`.`picture_url`, `user`.`username`
             , `cui`.`category`, `mat`.`material`;
        from `cul_article` AS `art`
            INNER JOIN `cul_user` AS `user`
                ON `user`.`id` = `art`.`author_id`
            INNER JOIN `cul_cuisine` AS `cui`
                ON `cui`.`id` = `art`.`cuisine_id`
            INNER JOIN `cul_material` AS `mat`
                ON `mat`.`id` = `art`.`material_id`
        where `art`.`author_id` = #{authorId};
    </select>

    <select id="selectPraise" resultType="java.lang.Integer">
        select `praise`
        from `cul_article`
        where `id` = #{id};
    </select>

    <select id="selectListByIsExamined" resultType="org.bearer.entity.vo.Article">
        select `art`.`id`, `art`.`title`, `art`.`picture_url`, `user`.`username`
             , `cui`.`category`, `mat`.`material`;
        from `cul_article` AS `art`
            INNER JOIN `cul_user` AS `user`
                ON `user`.`id` = `art`.`author_id`
            INNER JOIN `cul_cuisine` AS `cui`
                ON `cui`.`id` = `art`.`cuisine_id`
            INNER JOIN `cul_material` AS `mat`
                ON `mat`.`id` = `art`.`material_id`
        where `art`.`is_examined` = 0 LIMIT #{start},#{end};
    </select>

    <select id="selectCount" resultType="java.lang.Integer">
        select count(*)
        from `cul_article`
        where `is_examined` = 0;
    </select>

    <select id="selectArticleById" resultType="org.bearer.entity.vo.Article">
        select `art`.`id`, `art`.`title`, `art`.`picture_url`, `user`.`username`
             , `cui`.`category`, `mat`.`material`;
        from `cul_article` AS `art`
            INNER JOIN `cul_user` AS `user`
                ON `user`.`id` = `art`.`author_id`
            INNER JOIN `cul_cuisine` AS `cui`
                ON `cui`.`id` = `art`.`cuisine_id`
            INNER JOIN `cul_material` AS `mat`
                ON `mat`.`id` = `art`.`material_id`
        where `art`.`id` = #{id};
    </select>
</mapper>
