<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.bearer.mapper.MaterialMapper">

    <select id="selectList" resultType="org.bearer.entity.vo.Material">
        select `id`, `material`
        from `cul_material`;
    </select>
</mapper>
